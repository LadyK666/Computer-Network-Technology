# Public IP Traffic Map Synthetic Demo Data

This directory contains a sanitized 1000-packet IPv4 demo. IP addresses are globally-routable-looking demo values mapped by geo_overrides.csv, MAC addresses are local demo addresses, and payloads are synthetic zero bytes. These packets are not real network captures.

Run the web server and open http://127.0.0.1:8088/ to view the default demo.
