# 开源说明

本目录是待上传的实验项目脱敏版本。

## 已处理内容

- 未复制 `.venv/` 虚拟环境、`tools/` 下的 Npcap 安装包、`__pycache__/` 和临时文件。
- 未复制实验二真实抓包输出，保留 `实验二_Npcap抓包程序/outputs/.gitkeep` 作为目录占位。
- 实验三保留报告中使用的相对路径，但 `outputs/demo_public_1000.pcap` 及对应 CSV/JSON/HTML 已替换为合成演示数据。
- 合成演示数据包含 1000 个 IPv4 包，MAC 为本地管理演示地址，负载为合成零字节，不包含真实业务内容。
- `geo_overrides.csv` 中的地址为演示地址到地点的离线映射，用于保证地图无需联网也能展示。

## 保留的路径

报告中的运行路径保持不变，例如：

- `实验二_Npcap抓包程序/sniffer.py`
- `实验三_IP协议分析与流量统计/ip_traffic_analyzer.py`
- `实验三_IP协议分析与流量统计/traffic_map.py`
- `实验三_IP协议分析与流量统计/web_app/server.py`
- `实验三_IP协议分析与流量统计/outputs/demo_public_1000.pcap`
- `实验三_IP协议分析与流量统计/outputs/demo_public_1000/`

## 上传前建议

提交前可再次运行：

```powershell
rg -n "TOKEN|API_KEY|password|secret|密钥|10\.10\.|192\.168\.|00:74:9c|3a:7c" .
```

其中 `实验报告_完整Markdown版.md` 是按要求原样保留的报告文件。
